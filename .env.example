# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Database — runtime (pooler, port 6543) used by the app
# Settings → Database → Connection string → Transaction pooler
DATABASE_URL=

# Database — direct connection (port 5432) used ONLY by drizzle-kit migrations
# Settings → Database → Connection string → Session mode / Direct
DIRECT_URL=

# Anthropic (for agent scoring / summary features — added in week 3+)
ANTHROPIC_API_KEY=

# Stripe (added later, week 6)
# STRIPE_SECRET_KEY=
# STRIPE_WEBHOOK_SECRET=
# NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
